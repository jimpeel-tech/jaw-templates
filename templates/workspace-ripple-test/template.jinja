{#
JAW DEV TEST — DOCUMENT WORKSPACE / RIPPLE DOWN

Suggested test flow:
1. Clone this development template twice so you have two Documents.
2. In Document A, open a few Section and Function tabs.
3. In Document B, open a different set of Section and Function tabs.
4. Switch between the Document tabs. Each Document should restore only its own
   Section/Function workspace for this JAW session.
5. Make this Template Global, assign the same Global Template to both Documents,
   and repeat the switching test. The shared Template ID must not collapse the
   two Document workspaces into one.
6. Stage the Experience Section in one Document. Experience and Functions that
   are no longer reachable from that Document should close from its workspace.
7. Restore the staged Section. Nothing should reopen automatically; click it to
   open it again.
8. Optional Global test: make a Section or Function Global and reuse it. Removing
   one Document reference must not remove the shared resource from other Documents.

These comments are intentionally part of the editable source and should not render.
#}
% DEV TEST: LaTeX comments should also remain editable but not render.
\documentclass[10pt,letterpaper]{article}
\usepackage[letterpaper,margin=0.75in]{geometry}
\pagestyle{empty}
\begin{document}

{{ identity }}

\vspace{0.25in}
\hrule
\vspace{0.18in}

{{ experience }}

\vspace{0.25in}

{{ closing }}

\end{document}
