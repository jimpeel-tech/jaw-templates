{#
SECTION: welcome

A Section owns document-facing content. It can contain static text, Jinja,
JAW Objects, generation blocks, and Function references.

This Section calls target_summary(), which is a Function.
#}

Welcome to JAW Documents, {{ user.full_name }}.

This paragraph lives in a Section rather than the Template. The Template decides where the Section appears and how the surrounding document is formatted.

{{ target_summary() }}

Open this Section in the Workbench, then Ctrl+Click target_summary() to open its Function. The Function returns content only; presentation-level LaTeX stays in the Template.
